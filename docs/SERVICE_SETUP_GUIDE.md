# 🚀 Trading Bot - Systemd Service Setup Guide

Полное руководство по настройке автоматического запуска торгового бота через systemd.

---

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Подробная установка](#подробная-установка)
3. [Управление сервисом](#управление-сервисом)
4. [Мониторинг и логи](#мониторинг-и-логи)
5. [Устранение неполадок](#устранение-неполадок)
6. [Настройки сервиса](#настройки-сервиса)

---

## 🚀 Быстрый старт

### Установка сервиса (3 команды):

```bash
# 1. Установить сервис
./install-service.sh

# 2. Запустить бота
sudo systemctl start trading-bot

# 3. Проверить статус
sudo systemctl status trading-bot
```

**Готово!** Бот запущен и будет автоматически стартовать при загрузке системы.

---

## 📖 Подробная установка

### Шаг 1: Подготовка

Убедитесь, что:
- ✅ Python 3.10+ установлен
- ✅ Все зависимости установлены (`pip install -r requirements.txt`)
- ✅ Файл `.env` настроен
- ✅ PostgreSQL запущен
- ✅ Бот работает вручную (`python3 main.py --mode production`)

### Шаг 2: Остановить текущий бот (если запущен)

```bash
# Найти процесс бота
ps aux | grep "python.*main.py"

# Остановить процесс (замените PID на реальный)
kill <PID>

# Или используйте pkill
pkill -f "python.*main.py.*production"
```

### Шаг 3: Установить сервис

```bash
# Запустить установку
./install-service.sh
```

Скрипт выполнит:
- ✅ Копирование файла сервиса в `/etc/systemd/system/`
- ✅ Перезагрузку systemd daemon
- ✅ Включение автозапуска

### Шаг 4: Запустить сервис

```bash
sudo systemctl start trading-bot
```

### Шаг 5: Проверить статус

```bash
sudo systemctl status trading-bot
```

Вы должны увидеть:
```
● trading-bot.service - Crypto Trading Bot
     Loaded: loaded (/etc/systemd/system/trading-bot.service; enabled)
     Active: active (running) since ...
```

---

## 🎮 Управление сервисом

### Использование управляющего скрипта (рекомендуется):

```bash
# Запустить бота
./manage-service.sh start

# Остановить бота
./manage-service.sh stop

# Перезапустить бота
./manage-service.sh restart

# Проверить статус
./manage-service.sh status

# Посмотреть живые логи
./manage-service.sh logs

# Посмотреть логи за сегодня
./manage-service.sh logs-today

# Включить автозапуск
./manage-service.sh enable

# Отключить автозапуск
./manage-service.sh disable

# Удалить сервис
./manage-service.sh uninstall
```

### Использование systemctl напрямую:

```bash
# Управление
sudo systemctl start trading-bot       # Запустить
sudo systemctl stop trading-bot        # Остановить
sudo systemctl restart trading-bot     # Перезапустить
sudo systemctl reload trading-bot      # Перезагрузить конфиг

# Статус
sudo systemctl status trading-bot      # Текущий статус
sudo systemctl is-active trading-bot   # Запущен ли? (active/inactive)
sudo systemctl is-enabled trading-bot  # Автозапуск включен?

# Автозапуск
sudo systemctl enable trading-bot      # Включить автозапуск
sudo systemctl disable trading-bot     # Отключить автозапуск
```

---

## 📊 Мониторинг и логи

### Просмотр логов systemd:

```bash
# Живые логи (обновляются в реальном времени)
sudo journalctl -u trading-bot -f

# Последние 100 строк
sudo journalctl -u trading-bot -n 100

# Логи за сегодня
sudo journalctl -u trading-bot --since today

# Логи за последний час
sudo journalctl -u trading-bot --since "1 hour ago"

# Логи с определенного времени
sudo journalctl -u trading-bot --since "2025-10-30 08:00:00"

# Только ошибки
sudo journalctl -u trading-bot -p err
```

### Просмотр файловых логов:

```bash
# Логи бота
tail -f logs/trading_bot.log

# Логи systemd output
tail -f logs/systemd-output.log

# Логи systemd errors
tail -f logs/systemd-error.log
```

### Мониторинг ресурсов:

```bash
# Использование CPU и памяти
systemctl status trading-bot

# Детальная информация о процессе
sudo systemctl show trading-bot

# Использование памяти
sudo systemctl show trading-bot -p MemoryCurrent
```

---

## 🔧 Устранение неполадок

### Проблема: Сервис не запускается

**Решение 1:** Проверить логи
```bash
sudo journalctl -u trading-bot -n 50
```

**Решение 2:** Проверить права доступа
```bash
ls -la /home/elcrypto/TradingBot/main.py
ls -la /home/elcrypto/TradingBot/.env
```

**Решение 3:** Запустить бота вручную для диагностики
```bash
cd /home/elcrypto/TradingBot
python3 main.py --mode production
```

### Проблема: Сервис падает и перезапускается

**Проверить:** Логи с ошибками
```bash
sudo journalctl -u trading-bot -p err -n 100
```

**Проверить:** Количество перезапусков
```bash
systemctl show trading-bot -p NRestarts
```

**Временно отключить автоперезапуск** для диагностики:
```bash
# Отредактировать сервис
sudo systemctl edit trading-bot

# Добавить:
[Service]
Restart=no

# Сохранить и перезагрузить
sudo systemctl daemon-reload
sudo systemctl restart trading-bot
```

### Проблема: Не читается .env файл

**Проверить:**
```bash
# Права доступа
ls -la .env

# Формат файла (должен быть KEY=VALUE без пробелов вокруг =)
cat .env | head -n 10
```

### Проблема: Превышен лимит памяти

**Увеличить лимит:**
```bash
# Отредактировать сервис
sudo nano /etc/systemd/system/trading-bot.service

# Изменить:
MemoryLimit=2G  # было 1G

# Перезагрузить
sudo systemctl daemon-reload
sudo systemctl restart trading-bot
```

---

## ⚙️ Настройки сервиса

### Файл: `/etc/systemd/system/trading-bot.service`

#### Основные параметры:

**Автоперезапуск:**
```ini
Restart=always              # Всегда перезапускать при падении
RestartSec=10               # Ждать 10 сек перед перезапуском
StartLimitInterval=200      # Окно времени для подсчета перезапусков
StartLimitBurst=5           # Максимум 5 перезапусков за StartLimitInterval
```

**Лимиты ресурсов:**
```ini
MemoryLimit=1G              # Максимум 1 GB RAM
CPUQuota=50%                # Максимум 50% CPU
```

**Безопасность:**
```ini
NoNewPrivileges=true        # Запретить повышение привилегий
PrivateTmp=true             # Изолированная /tmp директория
```

**Graceful shutdown:**
```ini
TimeoutStopSec=30           # Ждать 30 сек для graceful shutdown
KillMode=mixed              # Сначала SIGTERM, потом SIGKILL
KillSignal=SIGTERM          # Сигнал для остановки
```

### Изменение настроек:

```bash
# Метод 1: Отредактировать файл напрямую
sudo nano /etc/systemd/system/trading-bot.service

# Метод 2: Использовать systemctl edit (создает override)
sudo systemctl edit trading-bot

# После любого изменения:
sudo systemctl daemon-reload
sudo systemctl restart trading-bot
```

---

## 📝 Полезные команды

### Просмотр конфигурации:

```bash
# Показать полный конфиг сервиса
systemctl cat trading-bot

# Показать эффективную конфигурацию (с overrides)
systemctl show trading-bot
```

### Сброс состояния после ошибок:

```bash
# Сбросить failed state
sudo systemctl reset-failed trading-bot
```

### Проверка зависимостей:

```bash
# Какие сервисы должны стартовать до бота
systemctl list-dependencies trading-bot --reverse
```

---

## 🎯 Best Practices

1. **Всегда тестируйте вручную** перед установкой сервиса:
   ```bash
   python3 main.py --mode production
   ```

2. **Мониторьте логи** первые 5-10 минут после запуска:
   ```bash
   ./manage-service.sh logs
   ```

3. **Настройте алерты** на критические ошибки (через cron/systemd timer)

4. **Регулярно проверяйте статус**:
   ```bash
   ./manage-service.sh status
   ```

5. **Делайте бэкапы** перед обновлением:
   ```bash
   cp /etc/systemd/system/trading-bot.service \
      /etc/systemd/system/trading-bot.service.backup
   ```

---

## 📞 Troubleshooting Checklist

- [ ] Проверить логи: `sudo journalctl -u trading-bot -n 100`
- [ ] Проверить статус: `sudo systemctl status trading-bot`
- [ ] Проверить права на файлы: `ls -la main.py .env`
- [ ] Проверить PostgreSQL: `sudo systemctl status postgresql`
- [ ] Проверить .env файл: `cat .env | grep -v "^#" | grep "="`
- [ ] Запустить вручную: `python3 main.py --mode production`
- [ ] Проверить зависимости: `pip list | grep -E "ccxt|psycopg2"`
- [ ] Проверить сетевые подключения: `netstat -an | grep 5432`

---

## 🔗 Связанные файлы

- `trading-bot.service` - Файл конфигурации сервиса
- `install-service.sh` - Скрипт установки
- `manage-service.sh` - Скрипт управления
- `logs/systemd-output.log` - Лог stdout
- `logs/systemd-error.log` - Лог stderr

---

**Создано:** 2025-10-30
**Версия:** 1.0
