# ✅ Готово к установке!

Systemd сервис для автозапуска бота полностью настроен.

---

## 📦 Созданные файлы:

✅ `trading-bot.service` - конфигурация systemd сервиса
✅ `install-service.sh` - скрипт установки (исполняемый)
✅ `manage-service.sh` - скрипт управления (исполняемый)
✅ `SERVICE_SETUP_GUIDE.md` - полная документация
✅ `QUICKSTART_SERVICE.md` - быстрый старт

---

## 🚀 Установка за 3 шага:

### ШАГ 1: Остановить текущий бот (если запущен)

```bash
# Найти процесс
ps aux | grep "python.*main.py"

# Остановить (замените 9045 на реальный PID)
kill 9045

# Или используйте pkill
pkill -f "python.*main.py.*production"
```

### ШАГ 2: Установить сервис

```bash
./install-service.sh
```

### ШАГ 3: Запустить бота

```bash
sudo systemctl start trading-bot
```

---

## ✅ Проверка установки:

```bash
# Проверить статус
sudo systemctl status trading-bot

# Должны увидеть:
# Active: active (running)

# Смотреть логи в реальном времени
./manage-service.sh logs
```

---

## 🎮 Управление ботом после установки:

```bash
./manage-service.sh start       # Запустить
./manage-service.sh stop        # Остановить
./manage-service.sh restart     # Перезапустить
./manage-service.sh status      # Статус
./manage-service.sh logs        # Живые логи
```

Или через systemctl:

```bash
sudo systemctl start trading-bot
sudo systemctl stop trading-bot
sudo systemctl restart trading-bot
sudo systemctl status trading-bot
```

---

## 🔍 Что дает systemd сервис:

✅ **Автозапуск при загрузке** - бот запустится автоматически после перезагрузки сервера
✅ **Автоматический перезапуск** - если бот упадет, systemd перезапустит его через 10 секунд
✅ **Управление как системным сервисом** - стандартные команды start/stop/restart
✅ **Централизованные логи** - через journalctl
✅ **Ограничение ресурсов** - лимиты CPU (50%) и RAM (1GB)
✅ **Graceful shutdown** - корректная остановка с сохранением состояния
✅ **Зависимости** - ждет запуска PostgreSQL перед стартом

---

## 📊 Мониторинг:

```bash
# Живые логи systemd
sudo journalctl -u trading-bot -f

# Логи за сегодня
sudo journalctl -u trading-bot --since today

# Статус с использованием ресурсов
sudo systemctl status trading-bot

# Файловые логи бота
tail -f logs/trading_bot.log
```

---

## ⚠️ Важно:

1. **ПЕРЕД установкой** убедитесь, что бот работает вручную:
   ```bash
   python3 main.py --mode production
   ```

2. **Остановите** текущий процесс перед установкой сервиса

3. **Проверьте** PostgreSQL:
   ```bash
   sudo systemctl status postgresql
   ```

4. **Проверьте** .env файл:
   ```bash
   cat .env | grep -v "^#" | head -n 10
   ```

---

## 🆘 Если что-то пошло не так:

```bash
# Посмотреть ошибки
sudo journalctl -u trading-bot -p err -n 50

# Сбросить failed state
sudo systemctl reset-failed trading-bot

# Удалить сервис и начать заново
./manage-service.sh uninstall
```

---

## 📚 Дополнительная документация:

- `SERVICE_SETUP_GUIDE.md` - полное руководство (устранение неполадок, настройка)
- `QUICKSTART_SERVICE.md` - краткая шпаргалка

---

**Готовы начать? Запустите:**

```bash
./install-service.sh
```
