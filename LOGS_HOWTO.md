# 📜 Как смотреть логи бота

## 🚀 Самые простые способы:

### 1️⃣ Живые логи (обновляются в реальном времени):
```bash
tail -f logs/trading_bot.log
```
**Нажмите Ctrl+C для выхода**

### 2️⃣ Последние 50 строк:
```bash
tail -n 50 logs/trading_bot.log
```

### 3️⃣ Последние 100 строк:
```bash
tail -n 100 logs/trading_bot.log
```

### 4️⃣ Интерактивный просмотр:
```bash
./view-logs.sh
```
Выберите из меню, что хотите посмотреть.

---

## 🔍 Поиск в логах

### Найти ошибки:
```bash
grep -E "(ERROR|CRITICAL)" logs/trading_bot.log | tail -n 20
```

### Найти по символу (например BTCUSDT):
```bash
grep "BTCUSDT" logs/trading_bot.log | tail -n 20
```

### Найти открытие позиций:
```bash
grep "position_opened\|position_created" logs/trading_bot.log | tail -n 10
```

### Найти за сегодня:
```bash
grep "2025-10-30" logs/trading_bot.log | tail -n 50
```

### Найти за последний час:
```bash
grep "2025-10-30 10:" logs/trading_bot.log | tail -n 50
```

---

## 📊 Systemd логи (через journalctl):

### Последние логи сервиса:
```bash
sudo journalctl -u trading-bot -n 50
```

### Живые логи сервиса:
```bash
sudo journalctl -u trading-bot -f
```

### Логи за сегодня:
```bash
sudo journalctl -u trading-bot --since today
```

### Только ошибки:
```bash
sudo journalctl -u trading-bot -p err
```

---

## 📂 Все файлы логов:

```bash
ls -lh logs/
```

**Файлы:**
- `trading_bot.log` - **ОСНОВНОЙ ЛОГ БОТА** (смотрите этот!)
- `systemd-output.log` - stdout сервиса (обычно пустой)
- `systemd-error.log` - stderr сервиса

---

## 💡 Полезные команды

### Сколько места занимают логи:
```bash
du -sh logs/
```

### Очистить старые логи (осторожно!):
```bash
# Создать бэкап
cp logs/trading_bot.log logs/trading_bot.log.backup

# Очистить (удалит содержимое!)
> logs/trading_bot.log
```

### Посмотреть размер лога:
```bash
ls -lh logs/trading_bot.log
```

---

## 🎯 Рекомендации

**Для повседневного использования:**
```bash
# Живые логи - видеть что происходит сейчас
tail -f logs/trading_bot.log

# Последние 50 строк - быстрая проверка
tail -n 50 logs/trading_bot.log
```

**Для поиска проблем:**
```bash
# Ошибки
grep ERROR logs/trading_bot.log | tail -n 20

# Критические ошибки
grep CRITICAL logs/trading_bot.log | tail -n 20
```

**Для анализа:**
```bash
# Сколько позиций открыто сегодня
grep "position_created" logs/trading_bot.log | grep "2025-10-30" | wc -l

# Сколько ошибок за сегодня
grep ERROR logs/trading_bot.log | grep "2025-10-30" | wc -l
```

---

## 🆘 Если логи не обновляются

```bash
# Проверить, работает ли бот
./manage-service.sh status

# Проверить права на файлы
ls -la logs/

# Проверить когда лог обновлялся последний раз
stat logs/trading_bot.log | grep Modify
```

---

**Создан:** 2025-10-30
